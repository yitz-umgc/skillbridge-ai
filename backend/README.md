# SkillBridge AI Backend

This folder contains the FastAPI backend and AI skill-matching service for the SkillBridge AI Alpha release.

## Install Dependencies

From the `backend` folder, run:

```bash
python -m pip install -r requirements.txt
